Classify the difficulty of the red-team or attack-surface analysis request below into one bucket, by how much reasoning it needs.

Buckets:

- trivial — obvious, mechanical, or directly answerable from a single piece of evidence (version confirmation, single-point lookup, explanation of an explicit field, one known verification).
- moderate — a real but localized task (single-entry-point analysis, a limited call chain, a single state assumption, a simple verification artifact).
- hard — deep, cross-component, ambiguous, or attack-path and exploitability tasks requiring runtime falsification.

Reply with exactly one word: trivial, moderate, or hard.

Request:
{{prompt}}

Answer:
